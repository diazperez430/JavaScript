$hola=10;
_hola=10;

//Reglas para escribir nombres de variables o constantes//
/**1. No simbolos diferentes a $ o _ (Siempre al principio)
 * 2. Se recomienda usar camelCase, también existe el snake_case, HugaroCase
 * 3. Se recomienda usar todas las palabras o nombres en minúsculas para las variables
 * 4. Se recomienda usar todos los nombres en MAYUSCULAS para las constantes. 
 * 5. No usar como nombre las palabras reservadas del lenguaje
 * 6. No nombrar variables o funciones con el mismo nombre
 */

/** En JavaScript hay 2 maneras de definir una variable */
var dinero = 123.34;
let nombreProgramador = 'Tania';

/** En JavaScript la definición de una constante */
const PI = 3.1415;
const entero = Math.floor(dinero);

console.log(entero);

const resultado = 8+3*2;

/** El símbolo = es igual a una asignación */

